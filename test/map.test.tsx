import React from 'react';
import { create } from 'react-test-renderer';
// import Map from '../src/components/map/Map.component';

test('Map component with default values', () => {
    const MapComponent = create().toJSON();
});